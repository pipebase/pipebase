name: pipe_with_cycle
pipes:
  - name: a_source
    ty: Poller
    config:
      ty: PollerConfig
    output:
      data_ty: Integer
  - name: mapper_a
    ty: Mapper
    upstreams: [ "non_exists" ]
    config:
      ty: MapperConfig
    output:
      data_ty: Integer