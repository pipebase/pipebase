[
    {
        "cmd": "docker exec cassandra cqlsh -e \"CREATE KEYSPACE IF NOT EXISTS test WITH REPLICATION = {'class' : 'SimpleStrategy', 'replication_factor' : 1}\" localhost 9042",
        "description": "create keyspace",
        "retry": 1
    },
    {
        "cmd": "docker exec cassandra cqlsh -e \"CREATE TABLE IF NOT EXISTS test.records (key text PRIMARY KEY, value int, date date, timestamp timestamp )\" localhost 9042",
        "description": "create table",
        "retry": 1
    }
]