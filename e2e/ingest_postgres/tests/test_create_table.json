[
    {
        "cmd": "docker exec postgres psql pg_isready -d postgres -h localhost -p 5432 -U postgres",
        "description": "postgres is up",
        "retry": 3
    },
    {
        "cmd": "docker exec postgres psql -h localhost -p 5432 -U postgres -d postgres -w -c \"CREATE TABLE IF NOT EXISTS records ( key TEXT PRIMARY KEY, value INTEGER )\"",
        "description": "create table 'records'",
        "retry": 1
    }
]