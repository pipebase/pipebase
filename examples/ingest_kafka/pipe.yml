name: ingest_kafka
dependencies:
  - name: pipebase
    version: 0.1.2
    modules: ["pipebase::prelude::*"]
  - name: pipekafka
    version: 0.1.3
    modules: ["pipekafka::*"]
  - name: pipejson
    version: 0.1.2
    modules: ["pipejson::*"]
  - name: serde
    version: 1.0
    features: ["derive"]
    modules: ["serde::Deserialize", "serde::Serialize"]
  - name: pipewarp
    version: 0.1.2
    modules: ["pipewarp::*"]
pipes:
  - name: ingestion_server
    ty: Listener
    config:
      ty: WarpIngestionServerConfig
      path: catalogs/ingestion_server.yml
    output: UnsignedBytes
  - name: json
    ty: Mapper
    config:
      ty: JsonDeserConfig
    output:
      Object: Record
    upstreams: [ingestion_server]
  - name: record_converter
    ty: Mapper
    config:
      ty: KafkaJsonRecordConverterConfig
    upstreams: [json]
    output:
      Pair:
        lty: 
          Option:
            ty: String
        rty: UnsignedBytes
  - name: kafka_producer
    ty: Exporter
    config:
      ty: KafkaProducerConfig
      path: catalogs/kafka_producer.yml
    upstreams: [record_converter]
objects:
  - ty: Record
    metas:
      - derives: [Clone, Debug, Deserialize, Serialize, GroupAs]
    fields:
      - name: key
        ty: String
        metas:
          - tag: Group
      - name: value
        ty: UnsignedInteger